class Game {
    constructor() {
        this.lvlcounter = lvlcounter /// int
        this.level = this.setCurrentLevel()
    }

    setCurrentLevel() {
        /// set the level as first level for the first input
        if (this.lvlcounter === 1) {
            /// itterate to set the levels
        }
    }
    
}