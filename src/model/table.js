class Table {
    constructor(x, y, canvasCoordinates, document) {
        this.x = x
        this.y = y
        this.canvasCoordinates = canvasCoordinates /// in pixels
        this.document = document /// which document present
    }
    
}